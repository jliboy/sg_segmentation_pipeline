# Pipeline for stress granule segmentation and property extraction per cell

The main file is sg_pipeline.py. From this python code, you would be calling all other functions for processing steps such as nuclear, cell, and
stress granule segmentation. 

R code sg_data_plotting.R is a simple R code for plotting main data. 

